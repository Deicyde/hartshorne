---
declaration: def
origin: cited
---

# The homogeneous vanishing ideal

For `Y ⊆ ℙⁿ`, the *homogeneous ideal* `J(Y) ⊆ S` is the ideal generated by the
homogeneous `f ∈ S` vanishing at every point of `Y`. For an algebraic set `Y`
the *homogeneous coordinate ring* is `S(Y) = S/J(Y)`.

`J(Y)` is homogeneous by construction, and the order-reversing properties
mirroring Proposition 1.2 hold: `T₁ ⊆ T₂` gives `Z(T₁) ⊇ Z(T₂)`, `Y₁ ⊆ Y₂` gives
`J(Y₁) ⊇ J(Y₂)`, `J(Y₁ ∪ Y₂) = J(Y₁) ∩ J(Y₂)`, and `Z(J(Y)) = Ȳ` (Exercise 2.3).

Unlike `A(Y)`, the ring `S(Y)` is not an invariant of the variety: it depends on
the chosen embedding into projective space. Hartshorne flags this in §3 and it
is why Theorem 3.4 describes `𝒪_P` and `K(Y)` through graded localizations of
`S(Y)` rather than identifying `S(Y)` itself with anything intrinsic.

## Depends on

- [Projective algebraic sets](projective-algebraic-set.md)
- [The Zariski topology on projective space](projective-zariski-topology.md)

## Sources

- [Hartshorne I.2, definition of `J(Y)` and `S(Y)`, with Exercise 2.3 (pp. 10-11)](../../sources/hartshorne.md#i2)
